<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- The Basics -->
    <groupId>org.open</groupId>
    <artifactId>mywebapp</artifactId>
    <version>1.9-SNAPSHOT</version>
    <packaging>war</packaging>

    <!-- More Project Information -->
    <name>my-web-app</name>
    <description>this is sample web app, maven and jetty</description>
    <url>http://maven.apache.org</url>
    <inceptionYear>2012</inceptionYear> <!-- This is another good documentation point. It will at least help you remember where you have spent the last few years of your life -->
    <organization>
        <name>swapsapar open source</name>
        <url>https://github.com/swapsapar/simplewebapp</url>
    </organization>
    <!--licenses>...</licenses-->
    <!--developers>...</developers-->
    <!--contributors>...</contributors-->

    <!--parent>...</parent-->
    <!--modules>...</modules!-->
    <!--dependencyManagement>...</dependencyManagement-->

    <!--properties>...</properties-->

    <!-- Environment Settings -->
    <!--issueManagement>
        <system>Bugzilla</system>
        <url>http://127.0.0.1/bugzilla/</url>
    </issueManagement-->
    <!--ciManagement>
        <system>TeamCity</system>
        <url>http://127.0.0.1:8011/continuum</url>
    </ciManagement-->
    <!--mailingLists>...</mailingLists-->

    <scm>
        <!-- read only -->
        <connection>scm:git:git://github.com/swapsapar/simplewebapp.git</connection>

        <!-- write -->
        <developerConnection>scm:git:git@github.com:swapsapar/simplewebapp.git</developerConnection>

        <!-- browsable -->
        <!--url>https://github.com/swapsapar/simplewebapp</url-->
    </scm>

    <prerequisites>
        <maven>2.2.1</maven>
    </prerequisites>

    <!--repositories>...</repositories-->
    <!--pluginRepositories>...</pluginRepositories-->
    <distributionManagement>
        <repository>
            <id>release-repo</id>
            <name>Local File Release Repository</name>
            <url>file:/Users/ssapar/mywork/abhyas/simplewebapp-repo/releases</url>
        </repository>
        <snapshotRepository>
            <id>snapshot-repo</id>
            <name>Local File Snapshot Repository</name>
            <url>file:/Users/ssapar/mywork/abhyas/simplewebapp-repo/snapshot</url>
        </snapshotRepository>
    </distributionManagement>


    <dependencies>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>3.8.1</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <!-- Build Settings -->
    <build>
        <finalName>mywebapp</finalName>
        <plugins>
            <plugin>
                <groupId>org.mortbay.jetty</groupId>
                <artifactId>jetty-maven-plugin</artifactId>
                <version>7.2.0.RC0</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-release-plugin</artifactId>
                <version>2.2.1</version>
                <executions>
                    <execution>
                        <id>default</id>
                        <goals>
                            <goal>perform</goal>
                        </goals>
                        <configuration>
                            <!-- fir error Cannot execute mojo: resources. It requires a project with an existing pom.xml, but the build is not using one." -->
                            <pomFileName>${artifactId}/pom.xml</pomFileName>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <!--profiles>...</profiles-->

</project>
